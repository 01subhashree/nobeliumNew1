when we use git stash it remove the change from the document and store it.Then after using git stash apply ,it apply the remove part or content on the document.but we have to apply this in  a different file if we use same file then the command give error.git stash pop do the same thing .But when we use git pop it discard the remove content from stash list after applying, but in git stash apply method it store the remove content we can apply it somewhere else also.